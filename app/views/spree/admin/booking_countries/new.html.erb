<h1>New booking_country</h1>

<%= semantic_form_for([:admin,@booking_country]) do |f| %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', admin_booking_countries_path %>
